import 'package:flutter/widgets.dart';

class contact extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Here u can see your all contact"),
      )
    );
}
}