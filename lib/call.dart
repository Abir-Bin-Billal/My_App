import 'package:flutter/widgets.dart';

class call extends StatelessWidget{
  @override
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is ur call list"),

      ),
    );
  }
}